# Per-joint velocity / current limits. Values are a percentage (0-100)
# of the per-joint 100% reference (max velocity / current).
string name
uint8  velocity   # % of per-joint max velocity
uint8  effort     # % of per-joint max current
